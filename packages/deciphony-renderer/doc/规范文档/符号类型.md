### 小节符号

这类符号x轴位置不能轻易改变

+ 谱号，拍号，调号，音符 且分为前置、后置、均分状态

### 游离符号

这类符号通常绑定一两个元素，且坐标通常可以拖动改变

+ 附属符号
    + 单音符附属 这类符号特别多，变音符号，拍速等
    + 双音符附属 连音线 升降八度等
    + 单小节附属 Fine To Coda等
    + 双小节附属 volta
+ 生成符号
    + 符干，符尾，符杠。 musicScore数据中无法直接展示，需要用时值等逻辑判断是否会渲染