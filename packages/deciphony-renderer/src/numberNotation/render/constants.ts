/**
 * 渲染相关常量
 */
//======================================================通用======================================================

/** 变音符号与音符头之间的默认间距（与 measureHeight 的比值） */
export const ACCIDENTAL_NOTE_X_GAP = 0
/** 变音符号与小节顶部的默认间距（与 measureHeight 的比值） */
export const ACCIDENTAL_NOTE_Y_OFFSET = 1 / 8
/** 音符前谱号与音符头的间距（与 measureHeight 的比值） */
export const CLEF_NOTE_GAP_RATIO = 1 / 8;
/** 附点符号与音符头之间的默认间距（与 measureHeight 的比值） */
export const AUGMENTATION_DOT_X_GAP = 0;
/** 减时线偏移y值（与 measureHeight 的比值） */
export const REDUCE_LINE_Y_OFFSET = 0
/** 附点符号y值与音符中心点y值偏移（与 measureHeight 的比值） */
export const AUGMENTATION_DOT_Y_OFFSET = 1 / 8;
/** 一节高度（一线或一间的距离）比例 */
export const LINE_SPACING_RATIO = 1 / 8;
/** 符杠最大倾斜角度（度） */
export const BEAM_MAX_SLOPE_DEG = 15;
/** 最小符干高度相对小节高度的比例 */
export const MIN_STEM_HEIGHT_RATIO = 7 / 8;
/** 符杠单线粗细比例 */
export const BEAM_THICKNESS = 2 / 16;
/** 符杠多条线之间的空隙比例 */
export const BEAM_LINE_SPACING = 2 / 32;
/** 符干 y 值偏移量 */
export const STEM_Y_OFFSET = 0.05;
/** 前置调号x偏移（与 measureHeight 的比值）*/
export const KEY_SIGNATURE_F_X_OFFSET = 0;
/** 前置调号y偏移（与 measureHeight 的比值）*/
export const KEY_SIGNATURE_F_Y_OFFSET = 0;
/** 后置调号x偏移（与 measureHeight 的比值）*/
export const KEY_SIGNATURE_B_X_OFFSET = 0;
/** 后置调号y偏移（与 measureHeight 的比值）*/
export const KEY_SIGNATURE_B_Y_OFFSET = 0;
